package com.locked.shingranicommunity.search_map

import android.view.View
import androidx.lifecycle.ViewModel
import com.locked.shingranicommunity.common.ResourceProvider
import javax.inject.Inject

class ItemDetailsViewModel @Inject constructor(private val navigation: Navigation, private val resourceProvider: ResourceProvider) : ViewModel() {



    fun searchLocation(view: View, locationString: String){
    }
}